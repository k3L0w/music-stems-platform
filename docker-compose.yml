version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: music-stems-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-music_stems}
      POSTGRES_USER: ${POSTGRES_USER:-music_stems}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-music_stems}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: music-stems-redis
    restart: unless-stopped
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
